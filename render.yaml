services:
  - type: web
    name: tibbiyot-nlp
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
